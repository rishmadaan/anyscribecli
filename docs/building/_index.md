# Building Docs — Recent Entries

| Date | Type | Entry | TL;DR |
|------|------|-------|-------|
| 2026-08-09 | feature | [[journal/2026-08-09-user-facing-docs-rebuild.md\|User-facing docs rebuild — three doors + docs pipeline]] | `docs/user/*.md` restructured into three journeys (agent / dashboard / CLI): new `agents.md` (skill vs MCP as a capability split), installer-first `getting-started.md` with a keep-it-running chapter, `commands.md` with a "Where in the Web UI?" column plus a carved-out `troubleshooting.md`, honest cost-to-start table in `providers.md`, README shrunk to a pitch. Docs render at **commit time** (`scripts/build-docs.py` → committed `landing/docs/*.html`) and CI fails on stale HTML, version drift, or an MCP tool missing from `agents.md` (`scripts/check-docs.py`). Product fixes so the docs stop lying: fresh-Apple-Silicon `install.sh`, `[tray]` extra in both installers, `keys/status` counts local (kills the permanent setup banner), Groq wizard card, honest post-Shutdown copy, macOS open-at-login toggle guarded on the tray extra. |
| 2026-07-29 | feature | [[journal/2026-07-29-full-web-config-parity.md\|v0.15.1 full Web-UI config parity]] | Settings page un-hides provider/model (was gated behind "custom"), gains the "Next run" banner (`_resolved` on /api/config), and a Downloads & media section incl. a real instagram.browser select. Rule: nothing is terminal-only. |
| 2026-07-29 | feature | [[journal/2026-07-29-config-defaults-redesign.md\|v0.15.0 config-defaults redesign]] | `quality: custom` sentinel (provider picks stick), shared `core/config_set.py` setter + `core/resolve.py` resolver behind every surface, bare `scribe config` defaults dashboard + `--json` agent discovery, visible run line + notes, `extra_models.openrouter`, OpenAI default → `gpt-transcribe` with auto whisper-1 timestamp routing. Suite 308 → 406. |
| 2026-07-29 | feature | [[journal/2026-07-29-model-picker-and-market-refresh.md\|v0.14.0 per-provider model picker + market refresh]] | New `PROVIDER_MODELS` registry + `settings.provider_models` + `--model` across CLI/Web/MCP. Adds OpenAI `gpt-transcribe` (2026-07-28 release; cheaper + more accurate, but no segment timestamps so whisper-1 stays default), migrates Sarvam to `saaras:v3` (v2.5 endpoint deprecated upstream), fixes OpenRouter's dead default slug (`gpt-4o-audio-preview` removed upstream → `gpt-audio-mini`), adds Groq `whisper-large-v3` + local `large-v3-turbo`/`distil-large-v3.5`. |
| 2026-07-21 | decision | [[journal/2026-07-21-rename-to-anyscribe.md\|Rename anyscribecli → anyscribe]] | v0.16.0 renames five independent identities (PyPI dist, import package, GitHub repo, command, config dir). `anyscribe` is the primary command; `scribe`/`ascli` stay as permanent aliases. Two proven blockers shaped the sequence: pip's dependency-first uninstall order deletes the shared `bin/scribe` on upgrade (so the final `anyscribecli` shim re-declares `scribe`/`ascli`/`scribe-mcp` and installs last), and an empty `~/.anyscribe` created before migration strands keys forever (fixed by migrating at every app-home read choke point + tolerating an empty target). New `anyscribe migrate` is the one-shot user-facing net. |
| 2026-07-16 | refactor | [[journal/2026-07-16-ponytail-simplification-batch.md\|v0.13.4 ponytail simplification batch]] | Canonical PROVIDER_KEY_ENV map (fixes groq drift in preflight+MCP key checks); shared `_transcribe_chunked` on the provider base class; dead-code sweep; `websockets` dep dropped. |
| 2026-07-14 | feature | [[journal/2026-07-14-delete-provider-key.md\|Delete a saved provider API key from the Web UI]] | Settings → Providers can now remove a saved key, not just add/replace it. New `delete_env()` rewrites `.env` without the named vars; new `DELETE /api/keys/{provider}` also strips it from `os.environ`. Frontend adds a two-step "Remove key" → "Remove?" confirm in the expanded provider panel (shown only when a key exists), matching the existing Replace/teardown pattern. No CLI change. |
| 2026-07-04 | feature | [[journal/2026-07-04-tray-icon-and-agent-first-landing.md\|Tray icon (nano-banana template image) + agent-first landing repositioning]] | v0.13.1 + post-release main. Menu-bar pressure test showed the placeholder circle icon was invisible on dark bars; replaced with a nano-banana-generated waveform, post-processed to a macOS template image (`setTemplate_(True)` via pystray darwin internals). Landing + README repositioned around usage priority: AI agents first (Claude Code exchange, skill + 10 MCP tools), Web UI second (real `scribe ui` screenshot via headless Chrome), CLI last. Killed "Most people never need this section". Hosting switched to Vercel (`vercel.json`); Pages workflow removed. Gotcha recorded: headless Chrome clamps min window width (~500px), so mobile checks need an iframe probe, not `--window-size=375`. |
| 2026-07-04 | feature | [[journal/2026-07-04-tray-releases-landing.md\|Menu-bar tray companion, GitHub Releases automation, landing page unparked]] | v0.13.0. `scribe tray` (pystray icon supervising the web server as a subprocess, pidfile-guarded) + `scribe install-service`/`uninstall-service` (macOS launchd LaunchAgent). Live testing found a real bug: pystray's Cocoa event loop can swallow a plain `signal.signal` handler, so SIGTERM/SIGINT never reached Python — fixed with a blocked-signal + `signal.sigwait` watcher thread. Publish workflow now auto-creates a GitHub Release with generated notes per tag (39 historical tags backfilled). Landing page's parked v3 design executed + wired to a Pages deploy workflow (hosting enablement pending). Suite: 282 passed (was 242). |
| 2026-07-04 | feature | [[journal/2026-07-04-logs-timeout-download-progress.md\|`scribe logs`, `batch --timeout`, byte-level download progress, provider tests]] | v0.12.0. `scribe logs` reads the existing daily logs + recovery dir (no new logging system). `batch --timeout` wraps each URL in a ThreadPoolExecutor future — abandons rather than kills a timed-out thread. Web UI model downloads now show byte-level progress via an hf tqdm_class hook riding the existing status poll (no WebSocket) — closes the queued 0.9.x BACKLOG item. 41 new provider unit tests found and fixed a real bug: sargam.py dropped speaker 0's label (integer 0 falsy in an `or` chain). Suite: 242 passed (was 189). |
| 2026-07-03 | feature | [[journal/2026-07-03-audit-fixes-and-lifecycle.md\|Audit fixes + transcript lifecycle (dedup, delete)]] | v0.11.0. Audit fixed a download-command crash (shipped untested) and a Groq provider-test that never checked its key. Adds duplicate detection (frontmatter scan, no cache file, enforced in the orchestrator so all surfaces inherit it) + `--force`, `scribe rm` delete with index resync across CLI/Web UI/MCP, cooperative Web UI job-cancel, and the first orchestrator integration test. |
| 2026-06-29 | reference | [[journal/2026-06-29-hinglish-transcript-format-and-llm-consumption.md\|Hinglish transcript format & LLM consumption]] | Bake-off + research on the best Hinglish transcript format for LLMs. Verdict: store verbatim (not a translation) — translation is lossy/one-way; LLMs handle Hinglish well. Deepgram `hi-Latn` default is sound. Also records the Sarvam `saaras:v2.5` + 28s-chunk fix (0.10.1). |
| 2026-06-29 | feature | [[journal/2026-06-29-quality-picker.md\|Quality picker (accuracy ↔ cost) + Groq]] | Accuracy↔cost picker (accuracy/balanced/cost/free) that resolves to a provider like `--diarize` does. Adds a Groq provider, migrates ElevenLabs to `scribe_v2` (v1 removed 2026-07-09), and makes config loading tolerate version drift. v0.9.0. |
| 2026-06-27 | reference | [[journal/2026-06-27-transcription-landscape-and-config-audit.md\|Transcription landscape & configurability audit]] | Audit of what's configurable vs hard-coded, a mid-2026 survey of the transcription-provider market, and a prioritized gap analysis. Urgent: ElevenLabs `scribe_v1` is removed 2026-07-09 and scribe targets it. |
| 2026-05-29 | troubleshooting | [[journal/2026-05-29-web-upload-filename-preservation.md\|Web upload filename preservation]] | Web UI uploads now keep the original filename in an isolated temp subdirectory so transcript slugs come from the user's file name instead of a UUID. |
| 2026-05-28 | troubleshooting | [[journal/2026-05-28-ci-frontend-bundle-line-endings.md\|CI frontend bundle line endings]] | CI failed because the committed Vite JS hash came from a Windows line-ending build; the Linux runner produced a different hashed bundle, so static assets now use LF-normalized attributes. |
| 2026-05-28 | troubleshooting | [[journal/2026-05-28-publish-readiness-follow-up.md\|Publish-readiness follow-up audit]] | Follow-up audit aligned package metadata to the documented 0.8.4 release, fixed a dev-only npm advisory, added the missing publish-time frontend gate, and re-ran the publish checks. |
| 2026-05-28 | troubleshooting | [[journal/2026-05-28-release-workflow-sanity-audit.md\|Release workflow sanity audit]] | Release audit found stale release docs, a macOS-only version bump in release.sh, no pre-publish CI, and a stray v0.8.3 tag on an unmerged branch. |
| 2026-04-29 | decision | [[journal/2026-04-29-instagram-yt-dlp-migration.md\|v0.8.3 Instagram → yt-dlp migration]] | Replaced instaloader with yt-dlp for Instagram downloads. No more password on disk; rate-limit-prone `test_login()` probe gone. Browser-cookie auth via `--cookies-from-browser`; config schema migrated from `instagram.username`/`INSTAGRAM_PASSWORD` to `instagram.browser`. |
| 2026-04-20 | bugfix | [[journal/2026-04-20-v082-upload-autosubmit-fix.md\|v0.8.2 Fix local-file browse auto-submitting]] | Web UI "Browse local file" button was firing the transcription job the moment the upload finished, giving the user no chance to adjust options first. Post-upload now populates the URL field and focuses it, matching the URL-text path. Two-line change in `URLInput.tsx` plus a rebuilt static bundle. |
| 2026-04-18 | feature | [[journal/2026-04-18-v081-onboarding-parity.md\|v0.8.1 Three-surface onboarding parity + polish]] | Closes the gaps from v0.8.0. Three fully-sufficient onboarding flows — Web UI wizard (first-run modal), TUI `scribe onboard`, headless `scribe onboard --yes` — all converging on shared `core/onboard_headless.py`. Plus `scribe model reinstall`, download queue, live pip-install log in the Web UI, focus-trapped modals, always-visible Diagnose. New architecture-doc section making the shared-backend + asymmetric-surface rule explicit. |
| 2026-04-18 | feature | [[journal/2026-04-18-local-transcription-unified-setup.md\|v0.8.0 Local transcription: unified opt-in setup + model management]] | Local provider is now a first-class opt-in. New `scribe local {setup, status, teardown}` + `scribe model {list, pull, rm, info}` CLI groups (agentic-first, `--json` everywhere, explicit `--model` required). Web UI Settings gains a Set-up-or-Manage local card with LocalSetupModal. Green-dot lie fixed; `/api/providers/local/test` returns structured `checks`. Marker release for the agentic-CLI / Web-UI-primary shift. |
| 2026-04-18 | plan | [[journal/2026-04-18-menu-bar-tray-companion-plan.md\|Menu-bar tray companion — future plan (~v0.9.x)]] | Deferred. Sketch + pressure test for turning `scribe ui` into a click-to-open tray app via pystray + `[tray]` extra + launchd/systemd auto-start. Browser stays the UI surface — no Tauri/Electron. Riskiest piece (self-update across pipx/pip/venv) deferred to v2. |
| 2026-04-18 | project-note | [[journal/2026-04-18-language-picker-polish.md\|Language picker polish — v0.7.4.7.1 + v0.7.4.7.2]] | v0.7.4.7.1: clear input on focus so the `<datalist>` popup shows all options (fixed the "auto" filter bug). v0.7.4.7.2: rename `Diarize` → `Multi-speaker`, `diarized` → `with-speaker-labels` in UI only — wire values unchanged. |
| 2026-04-18 | feature | [[journal/2026-04-18-provider-language-lists.md\|Per-provider language picker + provider key clarity]] | Transcribe page now shows `· needs key` + Settings CTA for unconfigured providers; language is a per-provider `<datalist>` dropdown. Static lists in `providers/languages.py` exposed via `/api/providers/{name}/languages`. |
| 2026-04-18 | project-note | [[journal/2026-04-18-v0741-webui-settings-polish.md\|v0.7.4.1 Web UI Settings Polish]] | Inline provider API key setup in Settings; "General" → "Configure Defaults"; standalone Diarization toggle removed (auto-couples with output format, matches CLI). |
| 2026-04-18 | feature | [[journal/2026-04-18-web-ui-initial.md\|Web UI Initial Implementation]] | `scribe ui` — FastAPI backend + React SPA with WebSocket progress. Three views: transcribe, history, settings. 17 smoke tests. Core dependency (not optional). |
| 2026-04-16 | plan | [[web-ui-spec.md\|Web UI Frontend Spec]] | Architecture decision (local web UI → Tauri → Swift), full frontend spec: React+Vite SPA, 4 screens, API contract, WebSocket protocol, color system, responsive layout. |
| 2026-04-16 | project-note | [[journal/2026-04-16-v0723-deepgram-hi-latn-nova-fallback.md\|v0.7.2.2–v0.7.2.3 Deepgram hi-Latn Nova Fallback]] | Deepgram Nova 3 doesn't support hi-Latn — falls back to Nova model. Misleading API error. Skill defaults Hindi + speakers to `--diarize --language hi-Latn`. |
| 2026-04-16 | project-note | [[journal/2026-04-16-v072-config-set-keys-diarize-routing.md\|v0.7.2 API Key Config + Diarize Routing]] | `scribe config set` handles API keys (routes to .env). `--diarize` auto-switches to Deepgram when configured. Fixes OpenAI 25MB diarize limit for large files. |
| 2026-04-16 | project-note | [[journal/2026-04-16-v070-diarization-deepgram.md\|v0.7.0 Diarization + Deepgram]] | Added multi-speaker diarization with `--diarize` flag. New Deepgram Nova provider with native diarization + hi-Latn. Upgraded OpenAI (gpt-4o-transcribe-diarize) and Sarvam. New "diarized" output format with speaker-grouped turns. |
| 2026-04-05 | project-note | [[journal/2026-04-05-v060-scribe-rename-mcp.md\|v0.6.0 scribe Rename + MCP Server]] | Renamed CLI to `scribe` with bare-URL routing, added MCP server (9 tools), skill auto-install/update, published to PyPI. |
| 2026-04-01 | project-note | [[journal/2026-04-01-v054-windows-compatibility.md\|v0.5.4 Windows Compatibility]] | Fixed Windows by invoking yt-dlp as `python -m yt_dlp` instead of bare command, fixing Python detection, and using `sys.executable -m pip` for installs. |
| 2026-03-31 | project-note | [[journal/2026-03-31-v053-ytdlp-auto-update.md\|v0.5.3 yt-dlp Auto-Update]] | Auto-detect and update stale yt-dlp (>60 days old) before any download. Prevents 403 errors from YouTube streaming format changes. |
| 2026-03-30 | project-note | [[journal/2026-03-30-v050-052-workspace-pypi-flatten.md\|v0.5.0–v0.5.2 Workspace, PyPI, Flatten]] | Configurable workspace path, PyPI automation via GitHub Actions, media→downloads rename, flattened workspace structure (removed date folders). Three migrations, one release script. |
| 2026-03-30 | plan | [[plans/flatten-workspace-structure.md\|Flatten Workspace Structure]] | Remove YYYY-MM-DD date folders — flatten to sources/&lt;platform&gt;/&lt;slug&gt;.md. Auto-migrate existing files on first run. (Done in v0.5.2) |
| 2026-03-30 | project-note | [[journal/2026-03-30-v041-claude-code-skill.md\|v0.4.1 Claude Code Skill]] | Built a Claude Code skill that teaches Claude how to use ascli for end users. Bundled in package, distributed via install-skill command and onboard integration. |
| 2026-03-30 | feature | [[journal/2026-03-30-local-file-transcription.md\|Local File Transcription]] | Added local file transcription — mp3, mp4, m4a, wav, opus, ogg, flac, webm, aac, wma files. New LocalFileDownloader, updated CLI and vault writer. |
| 2026-03-30 | project-note | [[journal/2026-03-30-v032-onboard-reconfig-ux.md\|v0.3.2 Onboard Reconfig UX]] | onboard --force shows current settings, asks before overwriting. Instagram error message fix. |
| 2026-03-29 | reference | [[ops/pypi-guide.md\|PyPI Publishing Guide]] | How PyPI works, first-time setup, TestPyPI, API tokens, troubleshooting. |
| 2026-03-29 | reference | [[ops/release-checklist.md\|Release Checklist]] | Step-by-step for every release — version bump through PyPI publish and verification. |
| 2026-03-29 | reference | [[ops/whats-automated.md\|What's Automated vs Manual]] | Self-update system, doctor checks, and what remains manual (versioning, publishing, docs). |
| 2026-03-29 | project-note | [[journal/2026-03-29-v031-documentation-accuracy-audit.md\|v0.3.1 Documentation Accuracy Audit]] | Full doc audit — 16 issues fixed across README, user docs, building docs. Fake terminal output, wrong paths, missing flags. |
| 2026-03-27 | project-note | [[journal/2026-03-27-v030-download-media-ux.md\|v0.3.0 Download, Media, UX]] | Download command, media outside vault, arrow-key selectors, URL validation, Instagram password to .env |
| 2026-03-26 | project-note | [[journal/2026-03-26-v020-full-feature-build.md\|v0.2.0 Full Feature Build]] | Built all features — Instagram, 4 new providers, batch, config commands, full onboarding |
| 2026-03-26 | decision | [[journal/2026-03-26-initial-architecture.md\|Initial Architecture]] | Initial architecture for anyscribe MVP — layered pipeline with pluggable providers and downloaders |
