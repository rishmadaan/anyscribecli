# anyscribecli

CLI tool to download, transcribe, and convert video/audio to structured markdown.

## Install

```bash
pip install -e .
```

## Usage

```bash
ascli onboard                    # first-time setup
ascli transcribe <url>           # transcribe a video
ascli transcribe <url> --json    # output as JSON
```
